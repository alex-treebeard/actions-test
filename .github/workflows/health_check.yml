on:
  pull_request:
  push:

jobs:
  run_1:
    runs-on: ubuntu-latest
    name: Run notebooks on treebeard
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-python@v2
      - uses: treebeardtech/treebeard@fix-git
        with:
          docker-username: alextreebeard
          docker-password: "${{ secrets.DOCKER_PASSWORD }}"
          docker-image-name: treebeardtech/actions-test
   
  run_2:
    runs-on: ubuntu-latest
    name: Run notebooks on treebeard 2
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-python@v2
      - uses: treebeardtech/treebeard@b035006fab66f832e248eb647019625848f693f9
        with:
          docker-username: alextreebeard
          docker-password: "${{ secrets.DOCKER_PASSWORD }}"
          docker-image-name: treebeardtech/actions-test
